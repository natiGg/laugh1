import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:get/get.dart';

class Constants {
  static final defHeight = MediaQuery.of(Get.context!).size.height;
  static final defWidth = MediaQuery.of(Get.context!).size.height;
  static final Color primaryColor = Color(0xFF000080);
}
